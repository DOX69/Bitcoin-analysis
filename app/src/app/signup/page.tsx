import DemoAccess from '@/components/auth/DemoAccess';

export default function SignupPage() {
    return <DemoAccess message="Account creation is not available. The current dashboard is a read-only market data demo." />;
}
