# This is a Databricks asset bundle definition for dbx_raw_ingest.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dbx_raw_ingest
  uuid: 5a000346-9b36-4da2-acf8-1833b2f56a85

include:
  - resources/*.yml
  - resources/*/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-00120f69-4aad.cloud.databricks.com
    variables:
      catalog: dev
      schema: bronze
  prod:
    mode: production
    workspace:
      host: https://dbc-00120f69-4aad.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/ggrftm@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/ggrftm@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: prod
      schema: bronze
    permissions:
      - user_name: ggrftm@gmail.com
        level: CAN_MANAGE
