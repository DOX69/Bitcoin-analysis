import DemoAccess from '@/components/auth/DemoAccess';

export default function LoginPage() {
    return <DemoAccess message="Accounts and sign-in are not available. The market dashboard can be opened without credentials." />;
}
